import { AudioInputPipe } from './audio-input.pipe';

describe('AudioInputPipe', () => {
  it('create an instance', () => {
    const pipe = new AudioInputPipe();
    expect(pipe).toBeTruthy();
  });
});
