import { EditableOnBlurDirective } from './editable-on-blur.directive';

describe('EditableOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
