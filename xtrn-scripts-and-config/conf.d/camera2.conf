#/etc/motion/conf.d/camera2.conf
#
# This config file was generated by motion 4.3.1+git20210125-34091a7

###########################################################
# Configuration options specific to camera 2
############################################################

# User defined name for the camera.
# This name comprises the motionName as in application.yml, the nginx app name, the nginx stream name and the nginx recording name (colon separated). The last three are repeated for the low res stream
camera_name SideGate:live2:cam2:live2_rec:live2lo:cam2:live2lo_rec

# Numeric identifier for the camera.
camera_id 102

# The full URL of the network camera stream.
netcam_url rtsp://192.168.0.34:554/12 

# Image width in pixels.
width 640

# Image height in pixels.
height 352

# Text to be overlayed in the lower left corner of images
text_left Camera2

# Text to be overlayed in the lower right corner of images.
text_right Patio\n%Y-%m-%d\n%T-%q

# Target directory for pictures, snapshots and movies
target_dir /home/www-data/motion-hls2lo

# File name(without extension) for movies relative to target directory
movie_filename CAM02_%t-%v-%s

framerate 3

movie_extpipe_use off
; movie_extpipe ffmpeg -y -f rawvideo -pix_fmt yuv420p -video_size %wx%h -framerate %fps -i pipe:0 -vcodec libx264 -preset ultrafast -level 3.0 -start_number 0 -hls_time 10 -hls_list_size 0 -f hls %f.m3u8

movie_passthrough on

; movie_codec mkv
