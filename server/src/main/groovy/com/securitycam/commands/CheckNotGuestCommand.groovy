package com.securitycam.commands

class CheckNotGuestCommand {
    String userAccount = "" // Dummy parameter to reference in validation errors

    // This is not used to receive any parameters, it's just to verify the that user is not a guest (with the associated validator)
}
