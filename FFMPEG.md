### ffmpeg
ffmpeg version 8.0.1-3ubuntu2, which is (at the time of writing)the standard
version installable with apt on Ubuntu 26.04. It is used to link the cameras to the 
media server, while re-muxing from RTSP to fragmented MP4, as well as handling certain recordings
on the system.
